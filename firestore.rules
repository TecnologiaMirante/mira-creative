// firestore.rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite acesso total durante o desenvolvimento.
      allow read, write: if true;
    }
  }
}